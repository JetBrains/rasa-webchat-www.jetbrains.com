# 🎭 E2E тесты с Playwright

## Что это?

**End-to-End (E2E) тесты** - это тесты в **реальном браузере** с **реальным backend**. Они проверяют весь flow от начала до конца, как настоящий пользователь.

## 🏆 Почему Playwright?

**Playwright** - это современный best-practice стандарт для E2E тестов в 2024-2025:

✅ **Быстрый** - быстрее Cypress и Puppeteer
✅ **Стабильный** - auto-wait, нет flaky тестов
✅ **Мультибраузерный** - Chrome, Firefox, Safari, Mobile
✅ **Параллельность** - несколько тестов одновременно
✅ **Видео/скриншоты** - автоматически при падении
✅ **Network interception** - можно мокировать API
✅ **TypeScript** - полная поддержка

## 📦 Структура

```
e2e/
├── tests/
│   ├── widget-flow.spec.js    # Основные E2E тесты
│   └── widget-auth.spec.js    # OAuth авторизация тесты
├── helpers/
│   ├── auth-helper.js         # Базовые утилиты
│   └── auth-flow.js           # OAuth flow helpers
├── reports/                   # HTML отчеты (генерируются)
├── README.md                  # Основная документация
└── OAUTH_TESTING.md           # 🔐 Руководство по OAuth тестам

playwright.config.js           # Конфигурация Playwright
.env.example                   # Шаблон для credentials
```

## 🎯 Покрытые сценарии

### 1. ✅ Виджет загрузился
- Launcher button отображается
- Виджет открывается при клике
- Заголовок отображается корректно

### 2. ✅ Авторизация
- Auth placeholder без токена
- OAuth окно открывается
- Chat показывается после авторизации

**📖 См. [OAUTH_TESTING.md](./OAUTH_TESTING.md)** - полное руководство по тестированию OAuth

### 3. 🔄 Session Management (требует backend)
- WebSocket соединение
- session_request отправляется
- session_confirm получается

### 4. 🔄 Messaging (требует backend)
- Сообщение пользователя отправляется
- Ответ от бота получается
- Сообщения отображаются в чате

### 5. 🔄 Manual Refresh (требует backend)
- Popup подтверждения показывается
- /restart отправляется
- История очищается

### 6. ✅ Automatic Token Refresh
- Токены хранятся в localStorage
- Socket переподключается
- Auth screen при неудаче

### 7. ✅ Additional
- Close button работает
- Embedded режим
- Persistence при reload

## 🚀 Запуск тестов

### Первый запуск - установка браузеров
```bash
npx playwright install chromium
```

### Основные команды

```bash
# Запустить все E2E тесты
npm run test:e2e

# С UI (интерактивный режим) 👍 Рекомендуется!
npm run test:e2e:ui

# С видимым браузером (headful)
npm run test:e2e:headed

# Режим дебага (пошагово)
npm run test:e2e:debug

# Показать отчет последнего запуска
npm run test:e2e:report
```

### Что происходит при запуске?

1. **Playwright запускает `npm run stage`** - ваш dev сервер на localhost:8080
2. **Ждет пока сервер запустится** (до 120 секунд)
3. **Открывает Chrome браузер** (headless или headed)
4. **Выполняет тесты** один за другим
5. **Генерирует отчет** с видео/скриншотами при падении

## 📝 Написание новых тестов

### Базовый тест

```javascript
const { test, expect } = require('@playwright/test');

test('мой новый тест', async ({ page }) => {
  // Открываем страницу
  await page.goto('/');

  // Ждем элемент
  await page.waitForSelector('.my-element');

  // Кликаем
  await page.locator('.my-button').click();

  // Проверяем
  await expect(page.locator('.result')).toBeVisible();
  await expect(page.locator('.result')).toHaveText('Success');
});
```

### С авторизацией

```javascript
const { setAuthTokens } = require('../helpers/auth-helper');

test('тест с авторизацией', async ({ page }) => {
  await page.goto('/');

  // Устанавливаем токены
  await setAuthTokens(page);

  await page.reload();

  // Теперь пользователь авторизован
  await page.locator('.rw-launcher').click();
  await expect(page.locator('.rw-new-message')).toBeVisible();
});
```

### Перехват WebSocket

```javascript
test('проверка WebSocket', async ({ page }) => {
  const messages = [];

  page.on('websocket', ws => {
    ws.on('framesent', frame => {
      messages.push(JSON.parse(frame.payload));
    });
  });

  await page.goto('/');
  // ... действия

  expect(messages).toContainEqual(
    expect.objectContaining({ type: 'session_request' })
  );
});
```

## 🔧 Конфигурация

### playwright.config.js

```javascript
{
  baseURL: 'http://localhost:8080',  // Базовый URL
  timeout: 30000,                     // 30 сек на тест
  retries: 2,                         // Повтор при падении
  workers: 1,                         // Количество параллельных тестов

  webServer: {
    command: 'npm run stage',         // Запускает dev server
    url: 'http://localhost:8080',
    timeout: 120000                   // 2 минуты на запуск
  }
}
```

### Настройка для CI/CD

В CI (GitHub Actions, GitLab CI) используйте:

```bash
# CI режим - больше retries, меньше workers
CI=true npm run test:e2e
```

## 🐛 Troubleshooting

### Тесты падают с timeout
```bash
# Увеличьте timeout в playwright.config.js
timeout: 60 * 1000  // 60 секунд
```

### Dev server не запускается
```bash
# Проверьте что порт 8080 свободен
lsof -i :8080

# Запустите вручную
npm run stage

# Затем в другом терминале
npm run test:e2e
```

### Браузер не открывается
```bash
# Переустановите браузеры
npx playwright install --force
```

### Нужны логи WebSocket
```javascript
page.on('websocket', ws => {
  console.log('WS:', ws.url());
  ws.on('framereceived', frame => console.log('<<', frame.payload));
  ws.on('framesent', frame => console.log('>>', frame.payload));
});
```

## 📊 Отчеты

После запуска тестов:

```bash
# Открыть HTML отчет
npm run test:e2e:report
```

В отчете вы увидите:
- ✅/❌ Статус каждого теста
- ⏱️ Время выполнения
- 📸 Скриншоты при падении
- 🎥 Видео при падении
- 📝 Трейс (шаги теста)

## 🎓 Best Practices

### ✅ DO
- Используйте `page.locator()` вместо `page.$`
- Используйте `toBeVisible()` вместо `toBeTruthy()`
- Группируйте тесты через `test.describe()`
- Используйте helper функции для повторяющихся действий
- Тестируйте критичные пользовательские флоу
- Делайте тесты независимыми (каждый может работать отдельно)

### ❌ DON'T
- Не используйте `page.waitForTimeout()` без необходимости
- Не хардкодите селекторы - выносите в константы
- Не тестируйте implementation details - тестируйте поведение
- Не делайте тесты зависимыми друг от друга
- Не используйте `sleep` - используйте `waitFor*`

## 📚 Полезные ссылки

- [Playwright Docs](https://playwright.dev)
- [Best Practices](https://playwright.dev/docs/best-practices)
- [Locators](https://playwright.dev/docs/locators)
- [Assertions](https://playwright.dev/docs/test-assertions)
- [Test Generator](https://playwright.dev/docs/codegen) - генерирует тесты за вас!

## 🎬 Генерация тестов

Playwright может сгенерировать тесты автоматически!

```bash
# Открывает браузер и записывает ваши действия
npx playwright codegen http://localhost:8080

# Генерирует код теста в реальном времени
```

Это отличный способ быстро написать тесты!

## 🔐 Работа с реальным backend

Если у вас есть реальный Rasa backend:

```javascript
// В playwright.config.js укажите реальный backend URL
use: {
  baseURL: 'https://your-widget-url.com'
}

// В тестах можно проверять реальные ответы
test('получить ответ от бота', async ({ page }) => {
  await setAuthTokens(page);
  await page.goto('/');
  await page.locator('.rw-launcher').click();

  await page.locator('.rw-new-message').fill('привет');
  await page.locator('.rw-send').click();

  // Ждем реальный ответ от Rasa
  await expect(page.locator('.rw-message').last())
    .toContainText(/здравствуйте|привет/i, { timeout: 10000 });
});
```

## 🎯 Следующие шаги

1. ✅ Установили Playwright
2. ✅ Создали базовые тесты
3. 🔄 Подключите реальный backend
4. 🔄 Раскомментируйте `.skip` тесты
5. 🔄 Добавьте больше сценариев
6. 🔄 Настройте CI/CD для автозапуска

## 💡 Совет

Начните с **UI режима**:

```bash
npm run test:e2e:ui
```

Это интерактивный интерфейс где вы видите:
- Список всех тестов
- Можете запускать по одному
- Видите браузер в реальном времени
- Смотрите логи и ошибки
- Перезапускаете упавшие тесты

**Best way to learn Playwright!** 🚀
