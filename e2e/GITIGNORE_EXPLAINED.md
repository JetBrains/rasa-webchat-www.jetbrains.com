# 📝 Объяснение .gitignore для E2E тестов

## ❓ Зачем нужен .gitignore в e2e/.auth/?

**Короткий ответ:** Это **best practice** для защиты токенов!

### Проблема:
Файл `e2e/.auth/user.json` содержит **реальные токены авторизации**:
```json
{
  "cookies": [...],
  "origins": [{
    "localStorage": [
      { "name": "chat_token", "value": "eyJhbGci..." }  ← СЕКРЕТНЫЙ ТОКЕН!
    ]
  }]
}
```

Если закоммитить в git → токены попадут в публичный репозиторий → **SECURITY BREACH!** 🔥

### Решение: Два уровня защиты

#### 1️⃣ Главный `.gitignore` (корень проекта):
```gitignore
# Игнорируем все JSON файлы в .auth/
e2e/.auth/*.json

# НО! Сохраняем сам .gitignore файл внутри
!e2e/.auth/.gitignore
```

#### 2️⃣ Локальный `e2e/.auth/.gitignore`:
```gitignore
# Игнорируем все JSON в этой директории
*.json
```

---

## 🎯 Зачем ДВА .gitignore?

### Вариант 1: Только главный .gitignore
```gitignore
e2e/.auth/*.json
```
✅ Работает
❌ Если забыл добавить правило - файлы попадут в git

### Вариант 2: Два уровня (BEST PRACTICE)
```
.gitignore              ← Игнорирует e2e/.auth/*.json
e2e/.auth/.gitignore    ← Дополнительная защита: *.json
```
✅ Двойная защита
✅ Даже если забыл в главном - локальный защитит
✅ Индустриальный стандарт

---

## 📊 Индустриальные примеры:

### Google Firebase SDK:
```
.firebase/
  .gitignore    ← Игнорирует секреты
```

### AWS Amplify:
```
amplify/
  .config/
    .gitignore  ← Игнорирует credentials
```

### Playwright официальная документация:
```
test-results/
  .gitignore    ← Игнорирует временные файлы
```

---

## 🔐 Что должно быть в git:

### ✅ КОММИТИТЬ:
```
e2e/
├── .auth/
│   └── .gitignore       ✅ Защита от случайного коммита
├── helpers/             ✅ Вспомогательные функции
├── tests/               ✅ Тесты
├── README.md            ✅ Документация
└── QUICK_START.md       ✅ Инструкции
```

### ❌ НЕ КОММИТИТЬ:
```
e2e/
├── .auth/
│   └── user.json        ❌ ТОКЕНЫ!
├── reports/             ❌ Генерируются каждый раз
└── test-results/        ❌ Временные файлы
```

---

## 📝 Полная структура .gitignore для E2E:

### Главный `.gitignore`:
```gitignore
# ========================================
# E2E Testing (Playwright)
# ========================================

# Test reports (HTML отчёты)
e2e/reports/

# Playwright test results
test-results/
playwright-report/

# Playwright cache
.playwright/

# Auth sessions (содержат токены!)
e2e/.auth/*.json

# Но .gitignore внутри .auth коммитим
!e2e/.auth/.gitignore

# Backup files
*.bak
```

### `e2e/.auth/.gitignore`:
```gitignore
# Игнорируем все JSON файлы (токены)
*.json
```

---

## 🚨 Что делать если случайно закоммитил токены?

### 1. Удалить из истории git:
```bash
git rm --cached e2e/.auth/user.json
git commit -m "Remove leaked tokens"
```

### 2. Инвалидировать токены:
- Выйти из аккаунта
- Перелогиниться
- Создать новую сессию: `npx playwright test --headed -g "setup-auth"`

### 3. Для production: Rotate credentials!

---

## 💡 Проверка что токены не в git:

```bash
# Проверить что файл игнорируется
git check-ignore e2e/.auth/user.json
# Должно вывести: e2e/.auth/user.json ✅

# Проверить что нет токенов в истории
git log --all --full-history -- e2e/.auth/user.json
# Должно быть пусто ✅

# Посмотреть что будет закоммичено
git status e2e/
# user.json НЕ должен быть в списке ✅
```

---

## 🎓 Итого: Best Practice

1. **Главный .gitignore** - игнорирует `e2e/.auth/*.json`
2. **Локальный e2e/.auth/.gitignore** - дополнительная защита `*.json`
3. **Коммитим** оба .gitignore файла
4. **НЕ коммитим** токены, reports, test-results

**Это индустриальный стандарт!** Используется в:
- Google (Firebase, GCP)
- AWS (Amplify)
- Microsoft (Playwright)
- Vercel, Netlify, и др.

---

## 📚 Дополнительно:

Если работаешь в команде, создай `.env.example`:
```bash
# Шаблон для команды (БЕЗ реальных значений)
TEST_USER_EMAIL=your-email@example.com
TEST_USER_PASSWORD=your-password
```

И игнорируй реальный `.env`:
```gitignore
.env           ← Реальные credentials
!.env.example  ← Шаблон (коммитим)
```

**Security first!** 🔒
