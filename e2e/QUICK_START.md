# 🚀 Quick Start: Запуск E2E тестов

## TL;DR (Быстрый старт)

```bash
# 1. ПЕРВЫЙ РАЗ: Авторизуйся (один раз в жизни!)
npx playwright test --headed -g "setup-auth"

# 2. Запускай тесты (сколько угодно раз)
npm run test:e2e:ui
```

Готово! 🎉

---

## Подробнее

### Шаг 1: Установка браузеров (только первый раз)

```bash
npx playwright install chromium
```

### Шаг 2: Создай сессию авторизации (только ОДИН РАЗ!)

```bash
npx playwright test --headed -g "setup-auth"
```

Что произойдёт:
1. Откроется браузер с виджетом
2. Popup для OAuth (Google/JetBrains)
3. **Авторизуешься своими руками** (у тебя 10 минут)
4. Popup закроется автоматически
5. Сессия сохранится в `e2e/.auth/user.json` 💾

**Больше не нужно авторизовываться!** Этот файл содержит токен.

### Шаг 3: Запускай тесты

#### Вариант A: UI режим (рекомендуется) 👍

```bash
npm run test:e2e:ui
```

Откроется интерфейс где:
- Видишь все тесты слева
- Запускаешь по одному или все сразу
- Смотришь результаты в реальном времени
- ✅ Зелёные галочки = тест прошёл
- ❌ Красные крестики = тест упал

#### Вариант B: Headless режим (для CI/CD)

```bash
npm run test:e2e
```

Все тесты запустятся невидимо, в конце покажет отчёт:
```
  15 passed
  1 skipped
  0 failed
```

#### Вариант C: Headed режим (с видимым браузером)

```bash
npm run test:e2e:headed
```

Видишь браузер, но без интерактивного UI.

---

## 📊 Структура тестов

### 1. **widget-auth-persistent.spec.js**
- `[setup-auth]` - Ручная авторизация (запускаешь ОДИН РАЗ вручную)
- `[with-saved-session]` - Тесты с сохранённой авторизацией

### 2. **widget-flow.spec.js**
- Основные E2E тесты виджета
- ✅ Работают с сохранённой сессией автоматически

### 3. **widget-auth.spec.js**
- Тесты OAuth flow
- ✅ Работают с сохранённой сессией автоматически

---

## 🔧 Troubleshooting

### Тесты падают с "Auth session required"

Значит не создана сессия. Запусти:
```bash
npx playwright test --headed -g "setup-auth"
```

### Токен протух (expired)

Удали старую сессию и создай новую:
```bash
rm e2e/.auth/user.json
npx playwright test --headed -g "setup-auth"
```

### Нужно тестировать с другим аккаунтом

Удали сессию и авторизуйся заново:
```bash
rm e2e/.auth/user.json
npx playwright test --headed -g "setup-auth"
```

### Хочу посмотреть отчёт прошлого запуска

```bash
npm run test:e2e:report
```

Откроется HTML отчёт с видео/скриншотами.

---

## 🎯 Best Practices

### ✅ DO:
- Запускай `setup-auth` **один раз**
- Используй `npm run test:e2e:ui` для локальной разработки
- Используй `npm run test:e2e` для CI/CD
- Коммить файл `e2e/.auth/.gitignore` (чтобы не залить токены в git)

### ❌ DON'T:
- Не коммить `e2e/.auth/user.json` в git (там токены!)
- Не запускать `setup-auth` в CI/CD (только локально)
- Не использовать production аккаунт для тестов

---

## 🔐 Безопасность

- `e2e/.auth/user.json` добавлен в `.gitignore` ✅
- Файл содержит токены - **НЕ коммить в git!**
- Для CI/CD используй переменные окружения или test API endpoint

---

## 📚 Дополнительная документация

- `e2e/README.md` - Полная документация по E2E тестам
- `e2e/MANUAL_AUTH.md` - Подробно про ручную авторизацию
- `e2e/OAUTH_TESTING.md` - Подробно про OAuth тестирование

---

## ⚡ Шпаргалка команд

```bash
# Первый запуск (один раз)
npx playwright test --headed -g "setup-auth"

# UI режим (лучший для разработки)
npm run test:e2e:ui

# Все тесты headless
npm run test:e2e

# Все тесты с видимым браузером
npm run test:e2e:headed

# Конкретный файл
npx playwright test e2e/tests/widget-flow.spec.js

# Конкретный тест по названию
npx playwright test -g "should send message"

# Debug режим (пошагово)
npm run test:e2e:debug

# Посмотреть отчёт
npm run test:e2e:report

# Удалить сохранённую сессию
rm e2e/.auth/user.json
```

---

**Готово! Теперь тесты работают с сохранённой авторизацией!** 🎉
