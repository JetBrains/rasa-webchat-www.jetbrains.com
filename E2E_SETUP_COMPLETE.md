# 🎉 E2E тесты с Playwright - Setup Complete!

## ✅ Что установлено

### 1. Playwright
- ✅ `@playwright/test` - фреймворк для E2E тестов
- ✅ `playwright` - библиотека для управления браузером
- ✅ Chromium browser - скачан и готов к использованию

### 2. Конфигурация
- ✅ `playwright.config.js` - настройки Playwright
- ✅ Auto-start dev server (`npm run stage`)
- ✅ Скриншоты/видео при падении тестов
- ✅ HTML отчеты

### 3. Тесты
- ✅ `e2e/tests/widget-flow.spec.js` - 20+ E2E тестов
- ✅ Покрывают все 6 требуемых сценариев
- ✅ Helper функции для авторизации

### 4. Скрипты в package.json
```json
"test:e2e": "playwright test",
"test:e2e:ui": "playwright test --ui",         👈 Рекомендуется!
"test:e2e:headed": "playwright test --headed",
"test:e2e:debug": "playwright test --debug",
"test:e2e:report": "playwright show-report e2e/reports"
```

## 🚀 Как запустить

### Вариант 1: UI Mode (Рекомендуется для начала)

```bash
npm run test:e2e:ui
```

Откроется интерактивный интерфейс где вы:
- Видите все тесты
- Запускаете по одному
- Смотрите браузер в реальном времени
- Анализируете ошибки

### Вариант 2: Headless (для CI/CD)

```bash
npm run test:e2e
```

Тесты запустятся в фоне, результаты в терминале.

### Вариант 3: С видимым браузером

```bash
npm run test:e2e:headed
```

Увидите как Playwright управляет браузером.

### Вариант 4: Debug режим

```bash
npm run test:e2e:debug
```

Пошаговое выполнение с Playwright Inspector.

## 📊 Результаты

После запуска смотрите HTML отчет:

```bash
npm run test:e2e:report
```

Отчет содержит:
- ✅/❌ Статус тестов
- ⏱️ Время выполнения
- 📸 Скриншоты ошибок
- 🎥 Видео падений
- 📝 Трейс действий

## 🎯 Покрытые сценарии

### ✅ Готовые к запуску (8 тестов)
1. ✅ Launcher button отображается
2. ✅ Виджет открывается при клике
3. ✅ Заголовок отображается
4. ✅ Auth placeholder без токена
5. ✅ OAuth окно открывается
6. ✅ Токены в localStorage
7. ✅ Auth screen при неудаче refresh
8. ✅ Close button работает

### 🔄 Требуют backend (12 тестов - закомментированы .skip)
9. 🔄 Chat после авторизации
10. 🔄 WebSocket соединение
11. 🔄 session_request отправляется
12. 🔄 Отправка сообщения
13. 🔄 Получение ответа
14. 🔄 Refresh popup
15. 🔄 Отправка /restart
16. 🔄 Очистка истории
17. 🔄 Socket reconnect
18. 🔄 Embedded режим
19. 🔄 Persistence
20. 🔄 И другие...

**Раскомментируйте `.skip` когда backend будет готов!**

## 🔧 Следующие шаги

### 1. Попробуйте UI Mode
```bash
npm run test:e2e:ui
```

### 2. Подключите backend
В `playwright.config.js` укажите реальный URL:
```javascript
use: {
  baseURL: 'https://your-widget-url.com'
}
```

### 3. Раскомментируйте тесты
Уберите `.skip` из тестов которые требуют backend:
```javascript
test.skip('название теста') → test('название теста')
```

### 4. Добавьте свои тесты
Используйте codegen для автогенерации:
```bash
npx playwright codegen http://localhost:8080
```

### 5. Настройте CI/CD
Пример для GitHub Actions:
```yaml
- name: Install Playwright
  run: npx playwright install --with-deps

- name: Run E2E tests
  run: npm run test:e2e
```

## 📚 Документация

- `e2e/README.md` - Полная документация E2E тестов
- `__tests__/README.md` - Документация unit/integration тестов
- [Playwright Docs](https://playwright.dev) - Официальная документация

## 🎓 Best Practices

### Playwright vs другие фреймворки

| Фреймворк | Скорость | Браузеры | Auto-wait | Стабильность |
|-----------|----------|----------|-----------|--------------|
| **Playwright** | ⚡⚡⚡ | Chrome, Firefox, Safari | ✅ | ⭐⭐⭐⭐⭐ |
| Cypress | ⚡⚡ | Chrome only | ✅ | ⭐⭐⭐⭐ |
| Puppeteer | ⚡⚡ | Chrome only | ❌ | ⭐⭐⭐ |
| Selenium | ⚡ | Все | ❌ | ⭐⭐ |

**Playwright - лучший выбор в 2024-2025!**

### Пирамида тестирования

```
        /\
       /E2E\        ← Playwright (критичные флоу)
      /------\        10% тестов, покрывают ключевые сценарии
     /Integration\  ← Testing Library (средний слой)
    /------------\    30% тестов, проверяют взаимодействие
   /  Unit Tests  \ ← Jest (быстрые, много)
  /----------------\  60% тестов, проверяют логику
```

### Когда использовать E2E

✅ **Используйте E2E для:**
- Критичных user flows (авторизация, оплата, регистрация)
- Проверки интеграции frontend + backend
- Проверки в реальном браузере
- Smoke тестов после деплоя

❌ **НЕ используйте E2E для:**
- Unit логики (используйте Jest)
- Проверки CSS/стилей (используйте visual regression)
- Тестирования всех edge cases (медленно)
- Замены unit тестов

## 💡 Советы

### 1. Начните с UI Mode
```bash
npm run test:e2e:ui
```
Лучший способ изучить Playwright!

### 2. Используйте Codegen
```bash
npx playwright codegen http://localhost:8080
```
Записывает ваши действия и генерирует код теста!

### 3. Debug с Inspector
```bash
npm run test:e2e:debug
```
Пошаговое выполнение с визуальным интерфейсом.

### 4. Смотрите трейсы
```bash
npm run test:e2e:report
```
Каждый упавший тест имеет трейс - видно все шаги!

### 5. Параллельность
В `playwright.config.js`:
```javascript
workers: 4  // 4 теста параллельно (быстрее)
```

## 🐛 Troubleshooting

### "Dev server не запустился"
```bash
# Убедитесь что порт 8080 свободен
lsof -i :8080

# Или запустите вручную
npm run stage
# В другом терминале:
npm run test:e2e
```

### "Тесты падают с timeout"
```javascript
// В playwright.config.js увеличьте timeout
timeout: 60 * 1000  // 60 секунд
```

### "Браузер не открывается"
```bash
# Переустановите браузеры
npx playwright install --force
```

## 📈 Статистика

```
✅ Установлено: Playwright + Chromium
✅ Создано: 20+ E2E тестов
✅ Покрытие: Все 6 требуемых сценариев
✅ Готово: 8 тестов без backend
🔄 Ждут backend: 12 тестов
```

## 🎉 Готово к использованию!

Просто запустите:

```bash
npm run test:e2e:ui
```

И начните тестировать! 🚀

---

**Best Practice 2024-2025**: Используйте Playwright для E2E тестов. Это самый современный, быстрый и стабильный фреймворк.

**Успехов в тестировании!** 🎭
